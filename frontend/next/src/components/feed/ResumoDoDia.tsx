import type { LedgerResumo } from "@/lib/ledgerApi";

/** Placeholder — Task 23 substitui pela versao real (spec §5, ResumoDoDia). */
export function ResumoDoDia(_: { resumo: LedgerResumo | null }) {
  return null;
}
