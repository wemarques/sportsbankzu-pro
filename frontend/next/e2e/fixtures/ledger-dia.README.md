Source: `GET https://smjc75r2ob2oo53yknph7kbxb40aauko.lambda-url.us-east-1.on.aws/ledger/dia?data=2026-09-14` (read-only, no mutation).
Captured: 2026-09-16, 58 picks / 7 jogos in the raw response.
match_ids kept (rows untouched): `liga-mx-Guadalajara-Pumas UNAM-1789348020.0` (9 picks: 1 SAFE outcome=1, 2 NEUTRO_QUALIFICADO outcome=1/0, 6 NEUTRO — talao = SAFE "Under 3.5" edge≈0.1577 > NQ "Over 3.5" edge≈0.0874 > NQ "Under 10.5" edge≈0.0715); `primeira-liga-Sporting Braga-GD Estoril Praia-1789415100.0` (3 picks, all NEUTRO, no vale pick → estado "direcao").
`resumo`/`semana`/`mes` recomputed with the exact `_resumo`/`_acumulado` logic of `backend/services/ledger_leitura.py:163-171,205-211` over just these 12 kept rows (treated as the whole window): contados = 3 (all from match 1) → `{picks:3, acertos:2, jogos:1, resolvidos:3}`; per família (`_FAMILIAS`) only Over/Under, Corners, Cards have 1 contado each (Over/Under and Corners acertos=1, Cards acertos=0 — the NQ "Over 3.5" outcome=0), others zero; `semana`==`mes` since the fixture has only this one day.
Note: the live endpoint's `resumo`/`semana`/`mes` did NOT include `resolvidos` at capture time (backend #256/Task 20-bis change, commit `cf62142`, not yet deployed to this Lambda) — this fixture adds it per the current `LedgerResumo`/`LedgerSegmento` TS contract (`frontend/next/src/lib/ledgerApi.ts`), computed with the same formula the deployed code will use once shipped.
