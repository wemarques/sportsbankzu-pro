# INDICE DE REGRAS — SportsBankZu Pro

> Uma linha por regra. Status **ATIVA** = regra permanente em `REGRAS_ATIVAS.md`.
> Detalhes completos em `REGISTRO_CORRECOES.md`.

| # | Status | Tipo | Resumo |
|---|--------|------|--------|
| 001 | Corrigido | Fix | Defesa contra odds alucinadas pela Mistral |
| 002 | Corrigido | Fix | Reincidencia Under 4.5 @1.95 alucinada |
| 003 | Corrigido | Feature | Integracao API-Football v3 |
| 004 | Corrigido | Fix | Auditoria API-Football precisao lesoes |
| 005 | Corrigido | Fix | Placar ao vivo congelado em 0-0 |
| 006 | **ATIVA** | Fix + Regra | Checklist 7 pontos para novo mercado |
| 007 | Corrigido | Fix | Livescore 0-0 ligas arabes |
| 008 | Corrigido | Fix | Placar "- : -" em jogos ao vivo |
| 009 | Corrigido | Fix | Jogo ao vivo excluido quando FootyStats remove |
| 010 | Corrigido | Fix | Status "started" nao reconhecido como "live" |
| 011 | Corrigido | Fix | Conformidade API-Football rate limit/timezone |
| 012 | Corrigido | Feature | Apostas em Sistema (System Bets) |
| 013 | Corrigido | Fix | Aba Duplas nao carrega jogos |
| 014 | Corrigido | Feature | Corner Progress Bar |
| 015 | Corrigido | Fix | CornerProgressBar invisivel Path B |
| 016 | Corrigido | Fix | Corners ausentes no contexto Mistral |
| 017 | Corrigido | Fix | Extracao stats ao vivo + vies BTTS |
| 018 | Corrigido | Fix | Remocao submodule worktrees CI |
| 019 | Corrigido | Fix | Liga dinamarquesa misturada + Duplas 504 |
| 020 | Corrigido | Fix | Auditoria duplas escanteios ERROU |
| 021 | Corrigido | Fix | Jogos dinamarqueses misturados EPL |
| 021a | Corrigido | Fix | CornerProgressBar ligas sem stats |
| 021b | Corrigido | Fix | Placar/minuto nao atualizam |
| 021c | Corrigido | Fix | CornerProgressBar parser robusto |
| 022 | Corrigido | Fix | League mismatch correcoes "ALL" |
| 023 | Corrigido | Fix | TypeScript period type |
| 024 | Corrigido | Feature | Filtros status, ordenacao, separador |
| 025 | Corrigido | Fix | Dropdown tema escuro + PL unknown |
| 026 | Corrigido | Fix | Jogos duplicados apelido vs nome |
| 027 | Corrigido | Fix | Fallback ID match indice numerico |
| 028 | **ATIVA** | Pipeline | Pipeline preditivo 5 camadas V2 |
| 029 | Corrigido | Feature | ML pipeline improvements |
| 030 | Corrigido | Feature | ML retrain validate workflows |
| 031 | Corrigido | Feature | Market Reference Signal governance |
| 032 | Corrigido | Fix | Live score stuck 0-0 |
| 033 | Corrigido | Feature | Corners Engine v2 bidirecional |
| 034 | Corrigido | Feature | Corner Betting Governance Framework |
| 035 | Corrigido | Feature | Ativacao Pipeline V2 M1-M6 |
| 036 | Corrigido | Fix | Deduplicacao mercados corners/gols |
| 037 | Corrigido | Fix | Formula overround Under + redundancia |
| 038 | Corrigido | Fix | Odds Under reais + enrichment |
| 039 | Corrigido | Feature | Prompt Mistral +40 campos |
| 040 | Corrigido | Feature | UI EV% real + redesign AI |
| 041 | Corrigido | Feature | Auditoria lambda root cause |
| 042 | **ATIVA** | Hard Constraint | Thresholds so com backtesting |
| 043 | **ATIVA** | Circuit Breaker | SAFE desativado + deflacao lambda 15% |
| 044 | Corrigido | Feature | AuditReportCard + League Confidence |
| 045 | Corrigido | Feature | Expansao 5->20 modelos binarios |
| 046 | Corrigido | Fix | Health/DB endpoint |
| 047 | Corrigido | Fix | Mistral timeout + Copa do Brasil |
| 048 | Corrigido | Fix | Live fallback "- : -" -> 0-0 |
| 049 | Corrigido | Fix | Format string bug market_models |
| 050 | Corrigido | Feature | Relatorio V3 backtesting |
| 051 | Corrigido | Fix | Lambda error null backtesting |
| 052 | Corrigido | Feature | Calibracao por liga |
| 053 | Corrigido | Fix | Lambda underestimation Dixon-Coles |
| 054 | Corrigido | Fix | BTTS calibration + SAFE save |
| 055 | Corrigido | Feature | Calibracao completa todos mercados |
| 056 | Corrigido | Fix | Extracao cards/corners/BTTS |
| 057 | Corrigido | Fix | Governanca testes documentacao |
| 058 | Corrigido | Feature | Deflacao per-league producao |
| 059 | Corrigido | Feature | Export/import calibracoes S3 |
| 060 | Corrigido | Fix | Live scores resilience cache |
| 061 | Corrigido | Fix | _prob() priorizar Poisson deflacionado |
| 062 | Corrigido | Fix | Team name mismatch EVs absurdos |
| 063 | Corrigido | Fix | EVs absurdos LAMBDA_MIN + fuzzy |
| 064 | Corrigido | Feature | Fallback temporada anterior |
| 065 | Corrigido | Fix | EVs absurdos 1X2 Poisson + cap |
| 066 | Corrigido | Fix | Ligas desaparecendo timeout fan-out |
| 067 | Corrigido | Fix | Live-score swap + proxy timeout |
| 068 | Corrigido | Fix | CornerProgressBar invisivel ao vivo |
| 069 | Corrigido | Fix | Auditoria Brier/SAFE per-league |
| 070 | Corrigido | Fix | /live-scores nao lia corners FootyStats |
| 071 | Corrigido | Fix | Jogos "VIVO 2T 90'" indefinidamente |
| 072 | Corrigido | Fix | Router AI Mistral mock |
| 073 | Corrigido | Fix | Migracao Mistral v3.7->v3.0 |
| 074 | Corrigido | Infra | Vercel Fluid Compute maxDuration |
| 075 | Corrigido | Fix | Endpoints auditoria 404 |
| 076 | Corrigido | Fix | /live-scores 0 jogos filtrados |
| 077 | Corrigido | Fix | CornerProgressBar visual + Brier |
| 078 | Corrigido | Feature | Dixon-Coles Complete Model |
| 078v | Corrigido | Fix | Validacao rho Dixon-Coles |
| 078r | Corrigido | Infra | Limpeza ligas 37->22 + recalibracao |
| 079 | **ATIVA** | Hard Constraint | MIN_N_BRIER=20, MIN_N_RELIABILITY=30 |
| 080 | Corrigido | Feature | Rename Classifications + Tooltips |
| 081 | Corrigido | Feature | Corners Engine v2 NB2 + barra 3 zonas |
| 082 | **ATIVA** | Contrato | Mistral EXCLUSIVAMENTE narrativa |
| 083 | Corrigido | Feature | Post-Match Diagnostic Engine V4.0 |
| 084 | Corrigido | Feature | Metricas no cron loop + baseline odds |
| 085 | Corrigido | Feature | Cartoes como mercado de picks |
| 085b | Corrigido | Fix | Avaliacao pos-jogo cartoes + NB2 v2 |
| 086 | Corrigido | Fix | Dupla contagem lambda cartoes |
| 087 | Corrigido | Fix | Standings ligas calendario-ano |
| 088 | Corrigido | Fix | Highlight odds pipeline |
| 089 | **ATIVA** | Fix + Regra | date_range deve usar BRT (nao UTC) |
| 089b | Corrigido | Fix | Dedup linhas cartoes |
| 090 | Corrigido | Fix | Fallback Mistral HTTP 400 |
| 090b | Corrigido | Fix | Auto-fetch AI ao selecionar jogo |
| 091 | **ATIVA** | Fix + Regra | Resolver alias ANTES de gerar match IDs |
| 092 | Corrigido | Fix | _extract_date BRT (mesma classe #089) |
| 093 | **ATIVA** | Safety | Mistral nao recomenda mercado sem odd |
| 094 | Corrigido | Feature | Bankroll editavel + Quarter Kelly |
| 095 | Corrigido | Feature | Odds reais cartoes API-Football |
| 096 | **ATIVA** | Safety | 6 camadas anti-alucinacao Mistral |
| 097 | Corrigido | Fix | Standings highlight fuzzy matching |
| 098 | **ATIVA** | Safety | Complementares >105% bloqueados |
| 099 | **ATIVA** | Safety | Filtro acoes auditoria por regras |
| 100 | Corrigido | Feature | ECE calibracao por faixa |
| 101 | Corrigido | Feature | Dashboard Confiabilidade Princeton |
| 102 | Corrigido | Feature | Tracker PostgreSQL + API clients |
| 102b | **ATIVA** | Infra | Checklist variaveis ambiente Lambda |
| 103 | **ATIVA** | Fix + Regra | book_odd obrigatorio para stake (nao odd_minima) |
| 104 | **ATIVA** | Fix + Regra | Corner lambda escala total (nao per-team) |
| 105 | **ATIVA** | Calibracao | Deflacao progressiva por banda + per-league |
| 106 | **ATIVA** | Fix + Regra | Raw prob para classificacao, deflated para EV |
| 107 | Corrigido | Feature | ELO service (coleta, sem integracao lambda) |
| 108 | **ATIVA** | Feature | EMA half-life=5 no lambda calculator |
| 109 | **ATIVA** | Feature | Brier Score automatico + persistencia |
| 110 | **ATIVA** | Feature | Scanner expandido 0.5-5.5 gols, 1.5-6.5 cards |
| 111 | Corrigido | Feature | 1X2/DC markets scanner |
| 112 | Corrigido | Perf | Cache EMA goals O(N) single pass |
| 112b | Corrigido | Infra | Lambda 1024MB + cache warm fix |
| 113 | **ATIVA** | Fix | Cron live + corredor cartoes + duplas + Under 2.5 |
| 114 | **ATIVA** (guarda no 203) | Infra | Lambda Function URL (elimina timeout 30s) — promovida a regra permanente em 2026-09-01 apos regressao silenciosa da config |
| 115 | Corrigido | Perf | ThreadPoolExecutor paralleliza jogos em batches |
| 116 | Corrigido | Fix | EV fixo +40.0% em escanteios — null EV when suspicious |
| 117 | Corrigido | Feature | Redesign aba Destaques — Valor Detectado + Apenas Analise |
| 118 | Corrigido | Fix | Cards Over 0% accuracy — totalCards=0 (campos nao populados) |
| 120 | Corrigido | Fix | Odds enrichment: correct fixture ID + expand O/U lines |
| 121 | Corrigido | Perf | API-Football cache TTLs otimizados — 8500 to ~3000 req/dia |
| 122 | Corrigido | Fix | Auto-finish incorreto quando /live-scores retorna vazio |
| 117b | Corrigido | Fix | DestaquesDoDia presets definem valor (nao somam) |
| 119 | **ATIVA** | Fix + Regra | Duplas: elegibilidade + correlacao + corredor + cap + diversidade |
| 119a | Corrigido | Perf | Loading progressivo frontend (onBatchReady, merge incremental) |
| 119b | Corrigido | Perf | Paralelizar FootyStats dentro da liga (ThreadPoolExecutor 3 workers) |
| 119c | Corrigido | Perf | Fan-out tuning (LEAGUES_PER_BATCH 3→5, MAX_CONCURRENT 4→2) |
| 119d | Corrigido | Perf | SQLite WAL mode cache FootyStats + API-Football |
| 119e | Corrigido | Fix | Lambda error /2 (media per-team, nao soma) |
| 119f | Corrigido | Fix | Backtest window 14→30 dias (N=15→437) |
| 119g | Corrigido | Perf | CORNER_BRIER_GRID granularidade fina 0.83-0.97 |
| 119h | Corrigido | Perf | CARDS_DEFLATION_GRID inclui inflacao >1.0 |
| 120 | **ATIVA** | Fix + Regra | Margem seguranca 5% selecao linha escanteios+gols |
| 121 | **ATIVA** | Fix + Regra | Enforce monotonicidade probs corners (P(Over N+1) <= P(Over N)) |
| 122 | **ATIVA** | Fix + Regra | Calibrador cards NB2 + Brier bilateral + override condicional + linhas expandidas |
| 123 | **ATIVA** | Fix + Regra | Direcao natural projFT + risk penalty + shrinkage assimetrico corners |
| 124 | **ATIVA** | Fix | Extrair cornersAgainst + cardsAgainst do FootyStats + fallback historico |
| 124b | Corrigido | Fix | xG blend conectado no pipeline de gols (calibrador → lambda) |
| 126 | **ATIVA** | Fix + Regra | Classificacao por direcao natural — gols, corners, cards (VIA 2) |
| 127 | **ATIVA** | Fix + Regra | VIA 2 antes do NO_BET + zona neutra proporcional + filtro Over 0.5 |
| 128 | Corrigido | Fix | Corner/card percentages mapeados + xG fallback None + calibrador extrai xG |
| 128e | **ATIVA** | Regra | xG blend so ativa com cobertura >= 80% home E away |
| 128i | **ATIVA** | Regra | Medicao obrigatoria Lambda Erro pre/pos recalibracao (limite 0.90) |
| 129b | **ATIVA** | Fix + Regra | Threshold Lambda Erro revisado 0.50 → 0.90 (benchmark Dixon-Coles) |
| 129c | **ATIVA** | Feature | Shadow Mode SAFE: computa internamente, exibe como NQ, loga para auditoria |
| 129e | Implantado | Infra | Pipedream webhook para notificacao shadow SAFE — email validado 09/04 23:11 |
| 130 | **ATIVA** | Fix + Regra | VIA 2 exige EV >= 0 para promocao a NEUTRO_QUALIFICADO |
| 131 | **ATIVA** | Fix + Dados | cornersTotalAVG + projFT ponderado (cross 35% + direct 30% + total 20% + league 15%) |
| 133 | Corrigido | Fix | V2 corners desbloqueado (matchesPlayed passado ao stats dict) |
| 134 | Corrigido | Fix | Legacy corners engine + cornersTotalAVG (terceira estimativa) |
| 137 | Corrigido | Fix | cards_engine denominador: league_avg → half_league (lambda era ~50% do real) |
| 138 | Corrigido (#139+#140) | Diagnose | Gaps silenciosos team_stats: ambas as camadas fechadas (mapper + resolver) |
| 139 | Corrigido | Fix | data_mapper canonical FootyStats field names: ~30+ campos com primary key season* + novos home/away splits |
| 140 | Corrigido | Fix | fix-133: 8 helpers de team_stat extraction roteados via _find_team_in_df (5-strategy fuzzy) |
| 128h | Corrigido | Fix | Recalibracao: xG blend ativado em 20 ligas, Brier O/U -0.0036 |
| 141 | Implementado | Feature | FootyStats /league-referees: referee avg cards wired into cards engine (referee_factor) |
| 142 | Implementado | Feature | FootyStats /lastx: recent form canonica (goals/cards/corners last5) → EMA/lambda calculator |
| 143 | Implementado | Feature | API-Football /fixtures/players: per-player cards/fouls para auditoria pos-jogo |
| 144 | Implementado | Feature | extract_best_odds: Corners O/U 4.5-12.5 desbloqueando scanner #110 |
| 145 | **ATIVA** | Fix + Regra | cornersRecorded_matches é match count, não média — projeção inflada 16→10.6 |
| 146 | **ATIVA** | Safety | Regra de EV obrigatório no prompt Mistral — NUNCA afirmar EV+ sem verificar prob × odd > 1 |
| 146b | **ATIVA** | Safety | Corredores devem ser apresentados como unidade (Over + Under = faixa esperada) |
| 147 | Implementado | Feature | Redesign Match Analysis + Rename VIÁVEL + Glossário + LiveTracker + CorridorCard |
| 148 | Implementado | Feature | Stake Kelly VIÁVEL (QK×0.30, cap 2%, floor 0.5%) + Badge resultado + Simulador banca |
| 149 | Implementado | Feature | Modo Oportunidade: stake por tier de classificação + desconto EV + barra exposição |
| 152 | **ATIVA** | Fix + Feature | Deflação BTTS reduzida (metade da banda) + monotonidade para cartões + rejected insights na UI |
| 153 | **ATIVA** | Fix | Complementar league-matches (page 1) com todays-matches para capturar todos os jogos da rodada |
| 154 | **ATIVA** | Fix | Paginar league-matches (todas as páginas, max_per_page=1000) para análise completa em todas as ligas |
| 155 | **ATIVA** | Fix | Mapear period API-Football→frontend (1H→1T, HT→HT, 2H→2T) + anular minute no HT |
| 156 | **ATIVA** | Pipeline | Deflation default 0.90 para ligas sem calibração per-league |
| 157 | **ATIVA** | Safety | Bloquear pares Double Chance antagonistas (DC 1X+DC 12, DC 1X+DC X2, DC 12+DC X2) |
| 158 | **ATIVA** | Fix | Direction rescue requer EV >= -5% — picks com EV muito negativo não podem ser "rescued" |
| 159 | **ATIVA** | Fix | Reliability N usa Brier acumulado (não batch único) — /health/reliability agora lê calculate_snapshot() |
| 160 | **ATIVA** | Infra | Late audit 02:00 BRT para jogos das Américas (Liga MX, MLS, Libertadores) que terminam após 23:45 BRT |
| 161 | **ATIVA** | Fix | Under-2.5 extra ×0.90 (#113) gated — skip quando `_DEFAULT_OU_DEFLATION < 1.0` ou liga em `_LEAGUE_DEFLATION` (evita dupla penalidade pós-#156) |
| 162 | **ATIVA** | Pipeline + UI | SAFE/NEUTRO accuracy = None quando sem picks (exibe "N/A" ao invés de "0.0%") + EV metrics no relatório (ev_medio_geral/positivo, picks_acionaveis, picks_ev_positivo) |
| 163 | **ATIVA** | Feature | Acurácia ponderada por 1/fair_odd — picks difíceis pesam mais; exposto via `compute_weighted_accuracy()` em `backtesting.py` |
| 161 | **ATIVA** | Fix | Gate Under-2.5 penalty ×0.90 quando lambda já deflacionado (#156). Evita ~19% deflação cumulativa matando gols |
| 162 | **ATIVA** | Fix+Feat | SAFE 0/0 exibe N/A (não 0%), compute_ev_summary() no relatório de auditoria |
| 163 | **ATIVA** | Feature | Accuracy ponderada (1/fair_odd) — favoritos pesam mais que underdogs |
| 164 | **ATIVA** | Fix | Logger `sportsbankzu` forçado a INFO (LOG_LEVEL env, fallback INFO) — AWS Lambda filtrava INFO via root handler em WARNING |
| 164b | **ATIVA** | Fix | Escopar logging à namespace sportsbankzu (sem root override) — evita spam boto3/urllib3/mangum no CloudWatch |
| 164c | **ATIVA** | Fix | Hook 5 V2-BUNDLES: chave `nome`→`mercado` + filtrar só `gols`/`btts` (exclui corners/cards do gols_total) |
| 165 | **ATIVA** | Fix + Feat | O/U half-band quando lambda pré-deflacionado (#156) + EV Floor 1% (EV_FLOOR_DROP) + Cards corridor dedup (sum>105% → maior EV) |
| 166 | **ATIVA** | Feature | Odds ingestion v2 — checklist break (home+over_25+btts_yes) + PRIORITY_BOOKMAKERS per-league + paginação 5→10 + endpoint debug gated. Flag `ODDS_INGESTION_V2` |
| 167 | **INFORMATIVA** | Investigação | MLS corners Brier 0.241 confirmado como floor do modelo atual — grid search rejeitou deflation 0.95 e safe_prob 0.75 |
| 169 | **ATIVA** | Guideline | Strict Contract (tipos/nulls/fallbacks em APIs externas) + First Principles (campos subutilizados = oportunidade de Brier) — complemento à Regra de Investigação |
| 170 | **ATIVA** | Feature | Corners diagnostic endpoint — 4 métricas empíricas (coverage homeAttackAdvantage, correlations, home×away, NB2 dispersion) para decisões sobre modelo de corners |
| 170-A | **ATIVA** | Pipeline | NB2 α corners calibrado per-league — resolve super-dispersão 5-30× detectada pelo #170 (MLS α_emp=0.033 vs α_prod=0.15, EPL α_emp=0.005). Flag `CORNERS_ALPHA_CALIBRATED` |
| 171 | **ATIVA** | Pipeline (P0) | Proteção banca pós-#170-A: ECE haircut (até -25%), OddsVal haircut (até -30%), market-family cap (corners 5%, goals 10%), daily loss circuit breaker (15%), `lambda_deflation` em ADJUSTMENT_LIMITS, env-var-gated auto-apply (AUTO_APPLY_CONFIDENCE_MIN=101) |
| 172 | Implementado | Infra | Vercel Build Minutes: Turbo→Standard + ignoreCommand com pathspec `:(top)` (~$580/mês → ~$58) |
| 173 | Implementado | Observabilidade | Medição degradação fim-de-temporada (script local) + snapshot diário de standings em S3 (cron 06:00 UTC) para backtest retroativo de features de contexto de temporada |
| 174 | Implementado | Fix + Política | Bug Report Card (null guards `safe_accuracy` em AuditReportCard.tsx, espelho do #168) + política de não-mexer com N=11 + watchlist Cartões Over 2.5 (calibrar α per-league se accuracy <40% com N≥15) |
| 175 | Implementado | FinOps | EC2 prognosticos-brasileirao-server t3.micro terminada (dark spend ~$90/ano, sem utilização) — captura de metadados em `infra/decommissioned/` para audit trail |
| 176 | Implementado | Security | FootyStats key rotacionada + helper `_redact_key` em `footstats_client.py` mascara `?key=`/`&key=` em 4 logger.* + retention temp 7d (restaurar 90d em 2026-05-08) |
| 177 | Implementado | Backend | Joint `by_league_market` JSONB no `brier_service` (MIN_N=5 diagnóstico + flag `diagnostic_only` quando N<20) — destrava validação red-team de subset (VALIDATE_REPORT 2026-05-04) |
| 178 | Implementado | UI + Backend | Brier target unificado em 0.22 (constante `BRIER_TARGET` em `brier_service.py`) + `model_beats_house_ci` com Wilcoxon p-value + N + badge `<ModelBeatsHouseRow>` no ReliabilityCard — destrava confiança operacional |
| 179 | Shadow Mode | Modelo | Recalibração shadow banda 50-60% (deflation 0.12→0.05) — observability via /metrics/shadow_v179, gate 2 semanas + 3% improvement; flag `SHADOW_BAND_50_60_V179` (default off) |
| 180 | Implementado | Backend + UI | Family pick selection (1 winner por família via `_market_family` reusado de #171; flag `ENABLE_FAMILY_SELECTION_180`; ranking por classification > EV > delta > band; annotate, not filter) |
| 181 | Implementado | Backend AI | Mistral input/output contract — proíbe citar probs raw e computar EV em narrativa; novo módulo `mistral_contract.py` com `validate_output` full-text + log-only observability (sem retry); prompt rules explícitas; documenta limitação estrutural da Camada 6 |
| 182 | Hotfix (P0) | Backend | Scipy lazy import em `brier_service.py` — top-level `from scipy.stats import wilcoxon` (#178) quebrou `/metrics/brier` em produção (`numpy._core.tests` ausente no Lambda Layer); import movido para dentro de `_with_ci()` em try/except. Fail-safe: `p_value=None` → `significant_at_5pct=False` |
| 183 | Implementado | Backend | Enrichment de matches via `get_match_details` — jogos via complement #153 agora podem ter `mercados[]` populado reusando `teams_df`/`league_df` do caller; rejeita record sem mercados (degrade gracefully); flag `ENABLE_MATCH_DETAILS_ENRICHMENT_183` default **false** (rollout pendente de validação empírica) |
| 184 | Implementado | Backend | xG filter None-guard — short-circuit em `ajustar_lambda_por_xg` quando games_played/goals_scored/xg=None + coerce na fronteira `ajustar_lambda_jogo_por_xg:218-225`. Cura raiz que #183 mascarava: outer except em `build_records_from_matches` derrubava silenciosamente records cujo team row faltava em teams_df. Label de skip-log corrigido (team_a_name vs home_team obsoleto) + warning observability em fixtures_service:1290. Smoke local: 3/6→6/6 jogos com mercados. |
| 186 | **ATIVA** | Fix + Feature | B-011 resolvido: date="week" invalidava TODO enrichment API-Football (guard Y-m-d + datas derivadas dos records via `_af_query_dates`); injuries/lineups consultavam /injuries com FootyStats id (ID space errado — nunca funcionou) → movidos para fixtures.py pós-matching com `apiFootballFixtureId` real; transparência de fontes: `_sources` no envelope /fixtures + `probSources`/`predictionSource` em stats; ladder O/U display com complemento e monotonicidade garantidos (`compute_ou_stats`); identidade lambda estrutural (`lambda_stats_block`); categoria Cards no market_reference_signal (não herda mais metadata over25 de gols) |
| 187 | **ATIVA** | Fix + Regra | Prioridades do pedido original: famílias de cartões/escanteios de bet único com validação de escada (elimina odds contaminadas); reclassificação de mercados pós-enrichment; 1X2 espelho ROTULADO na UI (decisão opção b, #028/#064); stats O/U de Poisson(lambdaTotalOU) e BTTS de (1-e^-λh)(1-e^-λa) — verificáveis pelo payload, reconecta feedback loop aos picks; calibrate_match_stats só corners; cartões >2.5 e escanteios >10.5 só com odd real (emenda #110); fonte fallback_default eliminada dos sinais |
| 188 | Fase 1/4 | Feature (infra IA) | Migração Qwen Fase 1: ai_audit_log (PG + fallback SQLite, log-and-degrade, psycopg2 lazy #182) com inputs/output/latência/valid_json/actual_result por jogo auditado; validador de schema (confidence 0-100, explanation não vazia, auditOdds condicional); hooks log-only no analyze_match + rotas; script de backfill de resultados. Zero mudança de comportamento; Mistral único em produção |
| 189 | **ATIVA** | Fix + Regra | Sugestão da AI reconciliada com a tabela de EV: Camada 7 (`_enforce_recommendation_contract`) substitui recomendação que cite mercado fora dos picks aprovados por `aligned_recommendation()` determinística (mesmos prob deflacionada/odd/EV do display); prompt #096 endurecido — cláusula "ou um mercado complementar" revogada + ramo "Sem recomendação" para jogos sem pick aprovado; `_MARKET_PATTERNS` cobre grafia inglesa `Double Chance` e `1X2 Home/Away` (regressão KRC Genk 2026-08-28: Sugestão exibia DC 1X "EV positivo, 87%" com a tabela listando EV -29.8%); stake sem odd bloqueado em `calcStakeOportunidade` (`odd<=1`, EV 0 era sentinela mentindo como medido) e `StakeRow` ocultada quando `bookOdd` ausente |
| 189-a/b/d | **ATIVA** | Fix + Regra | Pacote de cálculos (auditoria 2026-08-29): deflação CONTÍNUA por interpolação entre nós (`_DEFLATION_KNOTS`) substitui o degrau do #105 — monotonicidade de p·(1-d(p)) provada e testada em varredura 1e-4 (raw 60.0% exibia prob pior que 59.9%); #179 PROMOVIDO (nó 0.55 = 0.05, era 0.12; flag shadow inerte, /metrics/shadow_v179 reporta improvement 0); cross de cartões corrigido para fórmula simétrica (hf+aa+af+ha)/2 (viés -10% no λ eliminado) com guarda against>0 contra sentinela 0.0; floor #148 condicionado a EV≥0 — EV negativo vira ordem-limite `await_min_odd` com min_odd=fair nos dois modos de stake, backend e frontend |
| 189-e | **ATIVA** | Fix + Regra | Stake por família com edge comprovado (5.505 picks, Δ Brier vs mercado): cartões INFORMATIVOS (stake 0 em todos os modos/ligas até re-medição pós #189-b + árbitro), escanteios só linhas extremas (≥10.5/≤9.5), gols/1X2 plenos — `FAMILY_STAKE_POLICY` no bankroll_engine espelhada no frontend; calibração hierárquica mercado→família (pools por família em retrain, cartões finalmente em CALIBRATED_MARKETS); `_league_deflation_factor` resolve alias #185 antes do lookup (fator 0.90 do Brasileirão estava morto no fluxo do dashboard); migração idempotente unifica rótulos de liga fragmentados no audit_results; fetch diário restrito a `ACTIVE_LEAGUES()` (13 ativas, 9 desativadas) |
| 189-f | Implementado | Fix + Observabilidade | Odds de escanteios finalmente copiadas no enrichment (#120/#144: extraídas desde sempre, descartadas na cópia — todo pick de Escanteios nascia "sem odd", inutilizando as linhas extremas do gate #189-e); `_FOOTYSTATS_ODD_MAP` completo 4.5–12.5; fator de árbitro (#141) entra nos PICKS de cartões (lookup movido para antes de selecionar_mercados_v2, `refereeAvgCards` no record repassado ao predict_cards — pré-requisito da re-medição de cartões do #189-e); telemetria `[ODDS-COVERAGE]` por liga/família no CloudWatch (meta ≥70% com odd real) |
| 189-g | Implementado | UX | Camada visual do gate #189-e (display-only, zero cálculo): badge INFO cinza p/ picks de família sem stake (era "VIÁVEL" azul contradizendo o gate) + ProbBar dessaturada; AICard "analisando…" sem % falsa durante loading; contagem real de ligas no loading do dashboard; bloco de não-recomendados legível (motivo em chip, números tabulares); fmtMercado acentua rótulos só no display |
| 194 | Implementado | Fix (Live) | Relógio live fiel ao feed (liveClock.ts: feed como fonte única, interpolação ancorada com teto 3min, acréscimos/INT reais, useLiveTick 10s) + contrato do overlay (IDs canônicos footystatsId/apiFootballId, observedAtUnix/serverTimeUnix, minuteSource, nextUpdate 20s unificado com clamp no cliente) + crash corrigido no /live-scores (log lia detail_candidate.home antes do guard de None — um jogo ruim congelava placar/tempo de todos; teste de regressão) — commits rotulados "(#190)" pela sessão de origem |
| 195 | Implementado | Fix (Observabilidade) | Instrumentação da auditoria: `limit` exposto e repassado em /api/audit/status (default 10 truncava days=90 nos mesmos 10 registros); `calculate_snapshot(audit_date)` passa a filtrar por DATE(timestamp) de verdade + `daily_series()` e /metrics/brier/daily (o snapshot cumulativo dilui regressão recente em milhares de picks); `brier_picks` mede o mesmo conjunto que a acurácia (o tile "Brier Score Médio" media só Over 2.5 — 36,8% de acerto convivia com Brier 0.1137). `avg_brier_score` preservado: AuditReportCard baseia veredictos nele |
| 196 | Implementado | Fix (Métricas de EV) | Faixa de EV passa a usar o sinal (`abs()` punha EV -35% na faixa "20-100%", poluindo a faixa que decide se VALOR DETECTADO se paga) + faixas negativas + ROI e n_with_odds por faixa (40% de acerto @ odd 3.00 = +20% ROI: acurácia sozinha leva à conclusão oposta); auditoria passa a usar `merc["ev"]` e `calibrated_probability` em vez de recalcular de prob_max×odd_minima, e só grava `book_odd` com odd real (odd_minima vira 1/prob sem mercado — a "casa" do /metrics/brier era o próprio modelo, com delta zero); hit_rate_by_ev/roi/ev_metrics/sharpe_ratio passam a ser persistidos |
| 197 | Implementado | Fix (Crítico) | Red team do #195/#196: (a) o cron passa rótulo ("yesterday") onde o #195 esperava data ISO — Postgres recusava, except engolia, `persist_snapshot` nunca rodava e o brier_history parou de crescer em silêncio; `_normalize_audit_date` só aceita ISO e loga o resto, log de falha nomeia os parâmetros; (b) `_segment` pareava odds filtradas com desfechos NÃO filtrados (`outs[:len(odds)]`) — inócuo enquanto 98,8% dos picks tinham odd, viraria ruído com o #196 gravando book_odd só quando real (~1/3 sem); delta passa a comparar `brier_model_paired` × `brier_implied` no mesmo conjunto, com `n_paired` exposto |
| 198 | Implementado | Fix (Observabilidade) | 503 do /api/metrics/brier passa a expor `_error.kind` e `_latencyMs` (antes timeout, backend fora e erro de query eram indistinguíveis). Timeout mantido em 15s de propósito: o glob `functions` do vercel.json é `app/api/**` e as rotas vivem em `src/app/api/**` — subir o teto trocaria 503 com corpo por 504 de plataforma sem corpo |
| 199 | Implementado | Perf + Infra | /metrics/brier serve o último snapshot gravado (coluna JSONB `snapshot` em brier_history) em vez de recalcular tudo a cada carregamento do dashboard; `?fresh=true` força recálculo e o fallback cobre a primeira execução pós-deploy (sem migração manual — `ADD COLUMN IF NOT EXISTS` roda em `_ensure_table` antes do INSERT). Payload carrega `_fromHistory`/`_snapshotAt`/`_ageMinutes` para dado velho aparecer com etiqueta. Passthrough /api/metrics/brier/history criado (a rota do backend nunca teve caminho pelo front) |
| 201 | Implementado | Fix Sev 1 (Modelo) | (a) λ contava a vantagem de mando DUAS vezes — estatística casa-apenas normalizada por `average_goals_per_match/2`; passa a usar `seasonAVG_home/away` por lado (medido em 3.750 combos: λ casa −12,2%, λ fora +16,1%, razão H/A mediana 1,742→1,320, total −1,9%; 1X2 casa −8,5pp / fora +6,9pp — mandante estava +18,8% superestimado, e Over 2.5 mudava só −1,1pp, por isso o bug sobreviveu). Fallback ao comportamento anterior sem o campo ou fora da faixa 0,5–3,5; `_num()` para chave-com-None (#078v) e regressão de início de temporada por `games_played_home/away`. (b) train/serve skew do ML: as 4 features `*_avg_r5` eram lambdaHome/lambdaAway repetidos, zerando `attack_vs_defense`/`defense_vs_attack`; agora usam a série empírica de `_extract_lastx` (novo `goals_against_last5`) e o ensemble é PULADO sem série real. **Esperado pós-deploy:** ML mudo até o cache lastx expirar (TTL 120 min) — log `[Gap6] ML pulado` |
| 202 | Implementado | Fix (Métricas) | `run_after_audit` volta a gravar snapshot CUMULATIVO em brier_history — o #197 fez `audit_date` filtrar de verdade e o cron às vezes passa data ISO, então o batch das 05:01 gravou 170 picks (um dia) onde as linhas anteriores tinham 5.951 (acumulado), e o #199 serviu isso como global no ReliabilityCard. `audit_date` vira só etiqueta; o fatiamento por dia continua no `daily_series` (#195) |
| 200 | Implementado (mitigação) | Fix Sev 1 (Modelo) | Retreino automático de calibradores DESLIGADO — `retrain_all_calibrators()` aprendia de `audit_results`, que guarda prognóstico recomputado pós-jogo (Lecce×Roma publicou Under 2.5 / recomputou Over 2.5): picks que erram trocam de lado e somem da amostra, e a curva entra em produção via `calibrate_prob()` antes da classificação e do EV. Gate `_calibrator_retrain_enabled()` exige `CRON_AUTO_RETRAIN_CALIBRATORS=1`; semanal aceita `{"force": true}` por evento; batch expõe `calibrator_retrain_status`. **NÃO desfaz os .pkl existentes** (seguem sendo lidos; conjunto agora congelado — medir antes de quarentenar). **Causa raiz pendente:** ledger de picks publicados server-side (#192 fez no cliente). **FECHADO em 02/09 pelo #216:** medidos em produção, os `.pkl` congelados estão **INERTES** (`iso == crua` nas 25 linhas) — não há o que quarentenar; o gate segue valendo como prevenção |
| 203 | Guarda implementada (config pendente) | Fix (Infra) | `PY_BACKEND_URL` voltou a apontar para o API Gateway (teto duro de 30s), revertendo o #114: ligas COM jogos estouravam 30s e sumiam da tela enquanto ligas sem jogos respondiam em ~2s — o sintoma parecia "algumas ligas não carregam". Medido: championship 503@30,2s no gateway vs 200@34,3s na Function URL. Patch não troca a variável (config no Vercel); adiciona `isApiGatewayBackend()` com log no boot, `backendKind` no debug e mensagem que nomeia a causa em vez de "cold start". Regra #114 promovida ao REGRAS_ATIVAS |
| 204 | Implementado | Observabilidade | `LOG_LEVEL_MODELOS` (default WARNING, custo zero) eleva a árvore `backend.*` — 12 módulos usam `getLogger(__name__)` e ficavam fora do namespace `sportsbankzu` elevado pelo #164, herdando o root da Lambda em WARNING: todo `logger.info` deles era descartado. Foi por isso que a linha `Base Lado:` do #201 nunca apareceu no CloudWatch (o log não estava errado, estava mudo). Uma linha em main.py em vez de renomear 12 arquivos; opt-in porque /fixtures já opera perto do teto de tempo |
| 204b | Corrigido | Observabilidade | Regressão do #204: o default `WARNING` era inócuo na Lambda (basicConfig no-op, root já em WARNING) mas **rebaixava o dev local** (basicConfig funciona e põe root em INFO) — o commit que existia para destravar log apagou log no ambiente onde ele funcionava. Default passa a ser WARNING só com `AWS_LAMBDA_FUNCTION_NAME` presente; fora dela herda `_LOG_LEVEL`; env explícita vence os dois. Verificado nos 3 cenários |
| 205 | Corrigido | Fix (Deploy) | `VERCEL_FORCE_BUILD` como escotilha do Ignored Build Step: o guard só sabe ver diff de arquivo e é cego a mudança de variável de ambiente — os dois redeploys para aplicar o novo `PY_BACKEND_URL` (#203) foram cancelados contra o commit docs-only `a06c75e`, e a produção seguiu com o valor antigo embutido (`backendKind=api-gateway` no debug). Com a variável definida sai 1 antes de qualquer diff; sem ela o comportamento é idêntico. Verificado nos 4 caminhos, incluindo variável vazia (não força). Terceiro caso da família #193/#196 |
| 206 | Corrigido | Fix (Perf) | Fan-out de /fixtures passa a 1 liga por requisição (`LEAGUES_PER_BATCH` 3→1, `MAX_CONCURRENT` 2→4): o agrupamento de 3 fora dimensionado para o teto de 30s do gateway (#119c) e, removido esse teto pelo #203, virou gargalo — ligas pesadas não paralelizam dentro de uma invocação (championship+league-one juntas 52,7s vs 37,6s/33,9s separadas) e uma liga lenta derrubava as vizinhas (lote de 3 = 503 após 110,9s, com a premier-league de 1,6s sumindo junto). **Não resolve a lentidão:** ~30-35s por liga com jogos permanece; causa pendente é o cache SQLite do /tmp da Lambda não sobreviver entre invocações (2 chamadas /lastx por jogo pagas do zero toda vez) |
| 207 | Corrigido + instrumentado | Perf (Backend) | `get_shared_client()` — cliente FootyStats único por processo, thread-safe: `_fetch_lastx_for_team` criava um cliente NOVO por chamada (16 por pedido do championship), e cada `__init__` roda `_init_db()` com CREATE TABLE + commit, ou seja 16 transações de ESCRITA disputando o SQLite do /tmp sob os 3 workers do #115. Medido: ~4,5s/jogo linear mesmo com paralelismo (0 jogos 1,8s → 8 jogos 37,6s), enquanto uma chamada isolada custa ~1,8s. Entra também a linha `[#207 lastx]` por liga (times buscados/memoizados, soma, média, pior) no logger `sportsbankzu` — média ~1800ms aponta para a API, muito acima aponta para o SQLite. **Não mexer no paralelismo antes dessa leitura** |
| 208 | Corrigido | Fix (Modelo) | Encolhimento de amostra pequena passa a agir nos **dois** lados da conta: a regressão à média do #064 só valia abaixo de 5 jogos e **só no ataque** — a defesa do adversário, que é uma média de amostra idêntica, entrava crua com 1 ou com 30 jogos. Medido em produção (rodada de 01/09, 17 jogos): Championship e League One com 3 rodadas ficavam **−14,0pp / −13,5pp abaixo do mercado na casa**, contra −4,9pp (24 rodadas) e −0,7pp (25 rodadas). Limiar 5→8 (`LAMBDA_SHRINK_MIN_GAMES`), peso linear `n/8`. Medido em 5.625 combos: com 3 jogos razão λ 0,847→1,060 e P(casa) 34,2%→39,5%; **com ≥8 jogos |Δλ| = 0,000, byte a byte idêntico**. Contagem AUSENTE devolve peso 1,0 de propósito (ausência de informação não é informação de ausência); amostra vazia conhecida converge exatamente para os baselines da liga (1,50/1,15) |
| 209 | Implementado | QA (Estrutural) | Auditor de premissas sobre a **saída montada** do /fixtures — os 429 testes provam cada função isoladamente, ninguém verificava se o artefato obedece às premissas do modelo (foi o buraco de #201, #187 e #189-f, todos com etapas individualmente corretas). Nove premissas falsificáveis com o número que as falsifica: over/btts batem com o λ exibido e SAFE tem λ que sustente (CRÍTICO); fusão não ultrapassa o λ, λ longe do grampo, vantagem de mando existe na mediana, EV só com odd real (ALTO); EV alto é raro, early-season desliga (MÉDIO). **Matemática de referência reimplementada de propósito** — auditor que chama a função auditada concorda com ela até quando ela erra. Exit 1 com crítico / 0 sem, serve de gate de CI direto |
| 210 | Implementado | QA (Dívida de dados) | Manifesto dos 230 campos da FootyStats em três estados verificáveis (**102 consumidos / 96 planejados / 32 descartados**): 128 campos não tinham consumidor nenhum, incluindo `home_advantage_*` e `btts_percentage_home/away` — dado que o sistema derivava por outro caminho, e no caso do split casa/fora derivava ERRADO (causa do #201). Entra como 10ª premissa do #209, a única **estrutural** (roda sobre o código, não sobre a saída): campo consumido fora do manifesto, ou campo do manifesto sem consumidor, falha a auditoria |
| 211 | Corrigido | Fix (Diagnóstico) | Liga desconhecida devolvia **HTTP 200 com zero jogos**, sem log e sem campo no payload — indistinguível de "não há jogos hoje" (`championship` = 8 jogos em 35,4s; `england-championship`, que nunca existiu, = 0 jogos em 0,6s, lidos como rodada vazia). Causa é uma assimetria real do registro, não deslize: o `leagues.ts` mistura ID nu (`championship`, `premier-league`) com prefixado (`england-league-one`, `brazil-serie-a`). WARNING no log + `_ligas_desconhecidas` no payload nos dois caminhos, e premissa estrutural `ligas_do_frontend_resolvem` no auditor do #209 lendo `AVAILABLE_LEAGUES` contra `LEAGUES_CONFIG`/`LEAGUE_ID_ALIASES`. Colateral registrado em teste: **fora de produção o ID inválido devolve MOCK**, não vazio |
| 212 | Implementado | Observabilidade (Perf) | Instrumentação das fases do `/fixtures` depois que a leitura do #207 falsificou a própria hipótese (previa ~6000ms/chamada de `/lastx`; medido 234–378ms, 10–17% do pedido, e 0,3s com cache quente). Decomposição dos REPORT pelo marco `parallelizing`: dos 34,8–62,7s, **19,7–44,7s são ANTES do split** (busca da liga) e 15,1–18,0s no laço, dos quais só 0,3–6,1s são `/lastx` — sobram **11,9–14,8s de cálculo do modelo** por liga. Duas linhas: `season_ids/league_matches/stats+teams/ate_o_laco` e `laco_de_montagem`, relógio monotônico, logger `sportsbankzu`. **Não mexer em paralelismo nem em cache antes desta leitura** |
| 213 | Corrigido | Fix (Live) | **15 de 15 jogos de Championship e League One exibidos como 0-0 aos 80 minutos** com `/live-scores` vazio (01/09/2026, 20h20 UTC) — P(0-0 aos 80') = 10,8% por jogo, logo 1 em 300 trilhões para os 15. A linha de `league-matches` traz `homeGoalCount=0` até a partida terminar e o sistema, que promove o jogo a `live` pelo relógio, lia o zero como placar; o guard existente (com o comentário "Do NOT default live matches to 0-0" e a lembrança de Lanús×Boca 0-3) só cobria campo AUSENTE. `_placar_promovido` marca a promoção própria e faz o 0-0 daquela linha virar `score=null`, preservando gol real, 0-0 declarado pela fonte e 0-0 de jogo encerrado; o relógio continua (verificado: `live / score None / minute 65`). 12ª premissa do #209: rodada inteira 0-0 após o minuto 25 com 5+ jogos ao vivo é CRÍTICO |
| 214 | Implementado | QA (Odds) | Premissa de **margem de odds**: Londrina×Juventude trazia 1X2 2,88/2,70/2,17 = **17,8% de margem** enquanto a bet365 pagava 3,75/2,87/2,20 = 7,0% — e os outros mercados do MESMO payload estavam sãos (O/U 10,6%, BTTS 8,2%), logo é o 1X2 desatualizado, não a fonte. Como **todo EV é calculado contra essas odds**, a mesma probabilidade de 33,65% dava EV **−3,1% @2,88 e +26,2% @3,75** — sinal invertido só pela odd, o que contamina os +31%/+34% de escanteios igualmente. Faixa 1–12%: acima ALTO (odd velha), abaixo CRÍTICO (arbitragem impossível). Registrado sem corrigir: o λ de cartões é internamente coerente (NB2 λ=4,172, overdispersion 1,3 → 74,7%, exatamente o publicado) mas a **entrada** ignora o recorte casa/fora e usa 4,94 cartões/jogo numa liga reportada com 5,57 |
| 215 | Implementado | QA (Calibração) | **Âncora empírica da FootyStats como terceira opinião** sobre a pergunta aberta desde o #200 (os `.pkl` vazados ajudam ou atrapalham?): a distribuição por linha é contagem, não modelo, logo independente da crua e da calibrada. Londrina×Juventude em cartões — Over 2.5: empírico 82%, crua 74,7%, **calibrada 59,9% (afastou 22pp)**; Under 4.5: 54% / 60,9% / 53,0% (aproximou). Viés médio **−0,2pp na crua contra −11,6pp na calibrada em linhas de sentidos opostos** é a assinatura de deflação, não de calibração — mas 2 linhas não decidem, e o módulo mede sobre N jogos **sem decidir a quarentena**. Promove as 7 âncoras de cartões a CONSUMIDO (manifesto 102→109) e corrige o fallback histórico, que mediava só um lado do time (Juventude 1,88 contra 2,27 real; o Londrina batia por acaso). A premissa estrutural do #210 **pegou o primeiro defeito real** durante este patch: consumo por f-string não conta como consumo |
| 216 | Implementado | QA (Calibração) | **Separa o isotônico da deflação**, que `calibrated_probability` sempre misturou. A leitura do #215 (25 linhas em produção) deu "o calibrador AFASTA" — mas a saída mostrava `calibrada = 29,0 + 0,411 × crua` com **r=0,997** (ajuste afim, não escada de isotônico) e o próprio log trazia `calib_iso` **idêntico ao raw**: quem comprime é a deflação #105, por construção, não os `.pkl` do #200. `DetalheCalibracao` expõe os dois passos; `iso_probability` e `banda` viajam no payload (11 pontos de construção); trace estendido a 1X2/DC/escanteios/cartões, que não tinham nenhum e são os que levam banda **inteira** (`Cartoes Over 1.5` 0,8767→0,6575); comparador ganha coluna `iso`, contagem por banda e **veredito próprio do isotônico** (inerte vs. aproxima/afasta); `rsplit("/scripts/")` corrigido nos 2 scripts (quebrava no Windows). **Não quarentena nada** — entrega o instrumento que faltava |
| 217 | Corrigido | Fix (Publicação) | **O veto do motor de escanteios ganha consumidor.** `predict_corners` marcava `no_bet` em 4 situações e `grep no_bet` em `ev_classification` devolvia **zero** — o classificador lia `operationalState` como metadado e publicava assim mesmo (Preston e Sheffield no ar com EV +31,3% e +34,7% carregando RESTRICTED e LOW). Mesma família do #189-f. Veto distingue escopo: `insufficient_data`/`restricted_market` vetam a família, filtros de H2H e média de liga vetam só a linha e o lado; o pick vetado **não some** — vira NO_BET com `CORNER_ENGINE_NO_BET` e bloco `corner_veto`, para poder ser auditado. Junto: `detect_early_season` devolvia True com `matchesCompleted=None`, então `EARLY_SEASON_FALLBACK` disparava na rodada 24 **por campo ausente**, não por contagem baixa — `season_data_state` separa OK/EARLY/UNKNOWN. **Sem flag: muda comportamento no deploy** |
| 218 | Implementado | QA (Fonte limpa) | **Ledger imutável.** `audit_results` usa `INSERT OR REPLACE`, então guarda o prognóstico **recomputado pós-jogo** — a origem mecânica do vazamento que o #200 conteve com um gate. `prediction_ledger` (Postgres) só insere e grava as **entradas** junto da saída (dois λ, λ de cartões, amostra de cada lado, idade do dado) — a lacuna exata que obrigou o #216 a abrir uma quarta coluna. Unicidade por **conteúdo**: republicar idêntico não cria linha, republicar com qualquer número diferente cria, e o hash exclui `published_at` para o ledger não virar log de acesso. Não decide nada, falha aberto, `PREDICTION_LEDGER_ENABLED=0`. Teste verifica por inspeção de fonte que não há UPDATE/DELETE |
| 219 | Implementado | QA (Odds) | **Remoção de margem.** `backtesting.py:317` admite no código: "Implied probability = 1/odd (no overround normalization)" — todo EV compara o modelo com `1/odd`, que soma mais que 1, então perder para a margem e perder para a probabilidade eram indistinguíveis. Shin, proporcional e potência calculáveis lado a lado; divergência **medida** de 0,74pp no favorito com margem 17,8% e 0,47pp com 7,0% (a estimativa original era "2-3pp"). Mata a circularidade do `OVERROUND = 1.05/1.06`, que era margem **suposta** — de-vigar um par derivado dela devolve o que foi injetado. Fica **marcada sem correção** a circularidade maior: 1X2 tira probabilidade da própria odd, logo nunca aponta valor. `DEVIG_ENABLED=0` |
| 220 | Implementado | QA (Confiabilidade) | **Inclinação de calibração** — regressão logística do desfecho sobre `logit(p)`, sem binning (Murphy precisa de bin e a n=25-200 o termo de confiabilidade vira ruído). Nos dois pontos disponíveis a previsão varia **28,7pp** e a realidade **4,9pp**: inclinação **0,178**. Se confirmar, a conclusão **não é "calibrar melhor"** — isotônica, Platt e Beta são transformações monótonas de uma dimensão, e aplicadas a um sinal cego devolvem um sinal cego honestamente rotulado, com EV indo a zero. Bootstrap por **bloco=jogo** (Over 2.5 e BTTS do mesmo jogo compartilham o placar) e Benjamini-Hochberg para as 440 células. IRLS próprio, sem scipy (#182), com meio-passo e detecção **geométrica** de separação completa. Junto: `test_visual.py` importava streamlit no nível do módulo depois do `pytestmark` e derrubava a coleta da árvore inteira — **a suíte é de 753 testes, não 489; 264 nunca haviam rodado** |
| 221 | Corrigido | Fix (Dados) | **A contagem de jogos da liga chega ao consumidor.** Três camadas, três nomes: produtor monta `matches_completed` (`routes/fixtures.py:335`), consumidor pede `matchesCompleted`/`matches_played` (`data_governance.py:113`), e o portador (`league_avgs`) **não tinha nenhuma das duas** — `mp is None` em toda liga desde sempre, logo `DATA_MISSING` em 100% dos mercados, `EARLY_SEASON_FALLBACK` em todo pick e **zero SAFE** no payload (o EARLY_SEASON rebaixa SAFE). Portador passa a publicar os três rótulos e ambos os consumidores leem pelo mesmo `_jogos_disputados`. **`LEAGUE_MATCH_COUNT_ENABLED` nasce LIGADO** — aqui o estado atual é que é o errado —, com volta numa variável. **#221-a (achado na verificação):** havia uma QUARTA camada — o record nunca teve a chave `league_stats` que `ev_classification.py:884` lê, então o patch original saía **idêntico com e sem a contagem**; medido e corrigido. **Não resolve os escanteios:** o tier vem de `sample_adequacy` (amostra por time, porta dura em cada nível), e a contagem entra só no `league_risk`, peso 0,15 |
| 222 | **ATIVA** | Regra permanente (Engenharia / QA) | **Protocolo SDD — proibido afirmar efeito sem prova no payload montado.** A intenção do código não é o comportamento do sistema: é proibido afirmar em prompt, PR ou commit que um patch "remove flag X" ou "corrige cálculo Y" com base na leitura do código escrito — toda afirmação de efeito exige **medição empírica antes/depois** sobre o payload real. Exige a **auditoria dos 4 elos** de toda chave nova consumida via `.get()`: origem (onde o dado nasce), montagem (onde a chave entra no record), consumo (as linhas que a leem) e impacto real (diff no payload que o endpoint devolve). Critério de aceite falsificável declarado **antes** de editar arquivos, com teste que falhe antes e passe depois. **Payload idêntico antes/depois reprova o patch que AFIRMA efeito** — mas a recíproca não vale: instrumentação (#212) e funcionalidade atrás de flag desligada (#218/#219) declaram na Spec que não alteram número, e aí payload idêntico é o **aceite**. Cobre as **duas direções** (ler sem produtor e produzir sem leitor, #217) e proíbe teste que fabrica a própria entrada — o do #218 montava um `stats` com as chaves erradas e concordava com a suposição do código. Elos 2 e 3 das chaves do record passam a ser provados por `pytest tests/test_223_contrato_record.py`. Nasce do #221, que anunciava no próprio commit que o EARLY_SEASON sairia das ligas maduras e saía idêntico com e sem a contagem — faltava o quarto elo. Proibição 14 no CLAUDE.md. Verificação: `python3 scripts/testar_payload_diff.py --antes a.json --depois d.json` (exit 1 quando idêntico) |
| 223 | Implementado | QA (Mecanismo) | **Contrato das chaves do record** — `dict.get("X")` devolve None em silêncio, e é isso que permite ler o consumidor e concluir que o dado chega. Varre uma lista fechada de consumidores por `.get()` sobre `match_data`/`record`/`stats`/`league_stats`, monta records **reais** em 3 cenários e **bloqueia** toda chave lida e nunca escrita, salvo declarada em `OPCIONAIS` com motivo. Ao ser ligado: **29 lidas, 173 escritas, 9 bloqueios** — 3 falso positivo (nome legado em fallback encadeado, o varredor aprendeu a reconhecer) e **6 defeitos reais, todos no ledger do #218**: `homeMatchesPlayed`→`matchesPlayed_home`, `leagueMatchesCompleted` vive em `league_stats`, e `cardsLambda`/`expectedTotalCorners`/`dataAgeHours` **não existem em lugar nenhum**. O ledger gravaria **6 de 10 entradas nulas** — a lacuna que ele dizia fechar; corrigido para **2 de 14**, com chave presente nas duas. O teste do #218 também estava errado: construía um `stats` sintético com os nomes errados e **concordava com a suposição do código**. Torna o #222 executável — proibição é prosa, teste é gate |
| 225-a | Implementado (descartável) | QA (Instrumento) | **Rota de diagnóstico do histórico de odds** — `GET /api/debug/historico-odds`. Responde se as colunas `odds_ft_*` vêm **populadas** nos jogos passados: o manifesto (#210) diz que os campos existem, não que estão preenchidos, e muitas APIs zeram odds antigas. Se vierem preenchidas, o backfill do #225 reconstrói probabilidade **e** EV a partir de `get_all_league_matches` (aritmética local, não chamada de API) e o `medir_inclinacao.py` do #220 roda sobre milhares de observações sem esperar o ledger acumular; se não, sobra a probabilidade, que ainda responde "existe resolução?". Cobertura sobre **todos** os finalizados (amostra de 5 mente nos dois sentidos), só finalizados (odd de jogo futuro não prova nada sobre histórico), e lista as chaves `odds_*` reais do registro para denunciar nome diferente em vez de devolver nulo com cara de resposta. Odd 0 ou 1.0 = ausente. **Achado na verificação:** o teste do jogo futuro mutava `os.environ` e `_get_fsc` sem `monkeypatch` e **vazava para o resto da sessão** — corrigido |
| 225-a (adendo) | Corrigido | QA (Instrumento) | **O veredito de um campo condenava oito famílias.** Medido na championship (48 finalizados): 1X2 em **0%**, mas Over 2.5/3.5, BTTS e escanteios em **100%** — e a rota respondia "odds historicas AUSENTES" a partir de `odds_ft_home_team_win` sozinho. Mesma falácia de amostra pequena, noutro eixo: um campo mentindo sobre oito, e aceitá-lo mandaria o escopo do #225 para o lado errado. A leitura correta é quase oposta: backfill reconstrói **prob + EV** em gols e BTTS, perdendo só o 1X2 — a família onde o EV já era circular (#219). Segundo sinal: `home_team_corner_count` nulo com odd de escanteios em 100% — **odd sem desfecho não mede nada**, e a rota media a oferta sem medir a possibilidade de pontuá-la. Passa a ter veredito **por família** (`odd ≥50% E desfecho ≥50%`), `cobertura_desfechos` (0 é presente, só `None` é ausência) e lista de chaves que não esconde as `odds_ft_*` atrás do corte de 25 |
| 225-b | Corrigido | Fix (Escanteios) | **A cadeia de fallback do motor de escanteios estava morta.** `home_stats.get("corners_recorded_matches_num", home_stats.get("matchesPlayed_home", ...))` — mas `.get(k, alt)` só usa `alt` quando a chave está **ausente**, e `fixtures_service.py:1950` **sempre** cria essa chave, preenchida ou não. Com ela presente-e-nula o get devolve `None`, `_safe_float` dá 0, `min_sample=0`, `sample_adequacy=0`, tier `INSUFFICIENT` — e `_determine_governance_state` mapeia INSUFFICIENT direto para **RESTRICTED**. Era o piso que mantinha escanteios vetados com a temporada inteira jogada, a pergunta aberta do #221. Medido: `amostra 0.0 → 24.0`, `sample_adequacy 0.00 → 1.00`, `tier INSUFFICIENT → LOW`. **Não promove escanteios** — faz o fluxo chegar ao artefato de promoção em vez de ser barrado antes dele. Mesma forma de #201/#208/#217: ausência de informação tratada como informação de ausência. Descoberto ao investigar por que a contagem de escanteios vem 0/48 no histórico (#225-a) |
| 225-c | Corrigido | Fix (Classe de defeito) | **A varredura do `.get`: 52 fallbacks mortos, 51 corrigidos nos motores.** O #225-b fechou pedindo a varredura; esta é ela. O critério não é "falta tratar `None`" — é **`.get(k, alt)` cujo `k` vem de um produtor que sempre cria a chave**, caso em que a alternativa é inalcançável **por construção**. `scripts/varredura_get.py` monta os três records reais do contrato do #223, extrai as 172 chaves criadas em todos os cenários (95 já vistas nulas) e cruza por AST: **1990 `.get(k, default)` em `backend/`, 234 encadeados, 52 confirmados** → 1911/183/**38** depois do codemod. Quatro camadas: helper `backend/utils/valores.py` (`primeiro_valido`/`pegar`, que preservam `0`, `""` e `False` — `a or b` não); codemod AST com preservação de span em `corners/data_quality.py` (16), `cards_engine.py` (15), `corners_engine.py` (11) e `ev_classification.py` (9); guarda de regressão por AST em 7 módulos de decisão, com teste que verifica que a própria guarda ainda enxerga os arquivos; e conserto do detector do #223, que **o codemod quebrou** — casava sintaxe (`A.get("a", B.get("b"))`) em vez de semântica, então a forma nova fez nomes legados voltarem a contar como leitura primária. **Efeito medido (`scripts/ab_motores.py`, que carrega a versão ANTES direto do git):** nos records de referência a saída é **IDÊNTICA** nos 3 cenários — os 78%/75% de divergência aparecem só com injeção sintética de 40% de nulos, que é teto, não previsão. Não se afirma que produção muda; afirma-se e mede-se a remoção de 51 alternativas inalcançáveis. A forma divergente existe no feed real (o #225-b a mediu: `amostra 0 → 24`), mas a fixture não tem histórico de time para exercitá-la. **Fora de escopo com motivo — motivo depois refutado pelo #226:** aleguei que `corners/features.py` (13 confirmadas) exigiria codemod **+ retrain** por skew treino/serviço com `.pkl` persistidos. Não existe `.pkl` nenhum: o retrain semanal nunca rodou. Corrigido no #226. Restantes degradam narrativa/exibição, não decisão |
| 226 | Corrigido | Fix (Escanteios / Job morto) | **O retrain semanal de escanteios nunca treinou nada.** `_run_retrain_corners` importava `footstats_client.get_league_matches` (é **método** de `FootyStatsClient`, e recebe `season_id`, não slug) e `leagues_config.SUPPORTED_LEAGUES` (não existe — é `LEAGUES_CONFIG`), os dois no mesmo `try/except ImportError`. Medido: `{'status':'skipped'}` com a mensagem "footstats_client not available" — **com o módulo presente**. Consequência verificada: não existe nenhum `corner_regressor.pkl` nem `.corner_artifacts/`; o ML de escanteios nunca esteve ligado. Cinco camadas: coleta real e injetável (`coletar_partidas_escanteios`, com `resolve_season_ids` → `get_all_league_matches`, falha por liga isolada); `backend/utils/caminhos.py` (`DATA_ROOT` → `/tmp` na Lambda → `.` — `/var/task` é somente leitura e o `mkdir` do retrain levantaria `OSError`; três duplicatas viraram uma); codemod em `features.py` (44) e `retrain.py` (1 — `_extract_total_corners` zeraria **toda** partida se `totalCorners` viesse presente-e-nula), confirmadas 38 → **24**; correção de um bug que o próprio codemod introduziu (`ast.get_source_segment` de `(league_stats or {})` perde os parênteses → `league_stats or {}.get("x")` devolveria o **dicionário**), pego na revisão do diff e travado em `scripts/codemod_fallback.py`; e `n_matches`/`n_valid_corners` no resultado do cron, para `insufficient_data` distinguir "sem partidas" de "partidas sem contagem" (#225-a mediu 0/48). **Efeito medido no número publicado** (`scripts/ab_motores.py`): no record **real**, sem injeção, `home_possession`/`away_possession` **0.0 → 50.0** — a cadeia morta dava 0% de posse para os dois times, o que é impossível, e `poss_dominance = abs(p-50)/20` lia isso como domínio máximo; `pressure_index` **0.381 → 0.0238**, `expected_total_corners` **9.86 → 9.81**. **Prova de que treina:** `scripts/retreinar_escanteios.py --sintetico 200` → `completed`, 45 features, 3 modelos treinados, campeão selecionado, registry + primeiro `.pkl` que este pipeline já produziu. **Não resolve:** sem chave a coleta volta vazia (agora com motivo honesto por liga); a contagem de escanteios pode não vir no endpoint de liga (#225-a: 0/48 — aí o problema é a fonte); artefatos em `/tmp` na Lambda morrem com o container (persistir em S3 é item próprio). **Corrige o #225-c:** a justificativa de deixar `features.py` de fora ("skew treino/serviço com `.pkl`") estava errada — bastava um `find -name '*.pkl'`. Justificativa de escopo é afirmação sobre o sistema e exige a mesma medição que o resto (#222) |
| 226 (adendo) | Medido | QA (Escanteios) | **Primeira execução real: 605/1110 — e minha previsão estava errada.** championship, 2 temporadas: 1110 partidas coletadas, **605 com contagem de escanteios**, 553 amostras de feature, 45 features, NB/Poisson/ML todos treinados, campeão `negative_binomial`, 9 linhas em `NEUTRAL` (`force_shadow` funcionando). Eu havia escrito "minha expectativa honesta é a B" (`insufficient_data`, `n_valid_corners=0`), com base no 0/48 que o #225-a mediu. **Contradição:** `debug.py` mede `cobertura_desfechos` só em `home_team_corner_count`/`away_team_corner_count`, enquanto `_extract_total_corners` lê `team_a_corners` antes desses. Se a FootyStats publica em `team_a_corners` e deixa `*_corner_count` nula, as duas medições são consistentes — e é a mesma classe de erro do #225-a (um campo negando a existência do dado) noutro eixo. Hipótese resolvida por `scripts/diagnostico_chaves_escanteios.py`, que **descobre** os nomes (varre toda chave com "corner") em vez de presumir. **Reabre o escopo:** fechei o #225 com "escanteios estão fora — 0/48"; confirmada a hipótese, escanteios voltam com probabilidade **e** EV (odd já medida em 100%). **Em aberto de propósito:** versionar `.corner_artifacts/` e o `.pkl` levaria o modelo à Lambda (`DATA_ROOT` não setado = diretório do pacote) e mudaria o `predictor` em produção — decisão de governança, não efeito colateral de retrain |
| 226-b | Corrigido | Fix (Instrumento / Escopo) | **Não era ausência de dado, era ausência de nome — e o nome era nosso.** Medido em 605 finalizadas da championship: `totalCornerCount`, `team_a_corners`, `team_b_corners` em **100%**; toda a escada `odds_corners_*` (7.5–11.5, over e under, mais 1/x/2) em **100%**; timings em 98%. `home_team_corner_count`/`away_team_corner_count` **não aparecem na varredura** — não é chave vazia, é chave inexistente. Causa: `data_mapper.py:183` **cria** esses dois apelidos a partir de `team_a_corners`/`team_b_corners`; eles existem depois do mapper, e `debug.py` os media na linha **crua**, antes dele. Confusão de camada no nosso código, não peculiaridade da fonte — e pelo `.get()` chave vazia e chave inexistente são o mesmo `None`. Verificado que os outros consumidores estão certos (`fixtures_service` recebe `DataMapper.matches_to_df`, então `_avg_from_history` está vivo; `retrain`/`features` leem cru e listam `team_a_corners` primeiro) e que o manifesto #210 está certo ao declarar os apelidos CONSUMIDO. **Refuta o #225-a e a minha decisão de escopo:** fechei o #225 com "escanteios estão fora — 0/48". Escanteios têm a cobertura **mais rica das quatro famílias** — contagem 100% e odds 100% — melhor que 1X2, que tem desfecho mas odd em 0%. Voltam ao backfill com **prob + EV**. Corrigido em `debug.py` (3 listas), com guarda que lê os **literais** da rota por AST (não o texto bruto, senão o comentário que documenta o erro derrubaria o teste). Os testes do #225-a **fabricavam** o nome morto — regra #222 ponto 6 de novo, desta vez custando uma decisão de escopo. **Pergunta aberta:** `corners_potential` e `corners_o85/95/105_potential` em 100% mas com exemplo **0** — num campo de projeção 0 é enchimento, e `features.py` o usa como "âncora de projeção independente" (#123). Coluna não-zero adicionada ao diagnóstico responde na próxima execução |
| 227 | Implementado | Instrumento (Backfill) | **Backfill histórico: picks reconstruídos, com escanteios no escopo.** O #220 precisa de observações e o ledger (#218) está desligado; as observações já existem no histórico. `RastreadorHistorico` acumula por time o que aconteceu **até a partida anterior**, monta `stats` com as chaves que os motores **realmente leem** (auditadas contra `calcular_lambda_dinamico`, `estimate_corners_lambda`, `predict_cards`) e chama os motores de verdade. Média de zero observações devolve `None`, nunca `0` (#208); `-1` é ausência, `0` é resultado. **Escopo por medição:** gols O/U 0.5–4.5 (EV só em 2.5/3.5, as odds que existem), BTTS (prob+EV), **escanteios 7.5–11.5 (prob+EV, escada completa — reintegrados pelo #226-b)**, cartões 2.5–5.5 (só prob, não há coluna de odd). 1X2 fora inteiro: nossa prob de 1X2 **vem da odd** (#219) e a odd histórica está em 0% (#225-a). **Prova:** 400 partidas → 5070 picks (gols 2028, escanteios 1690, cartões 1352), 2704 com EV, alimentando `medir_inclinacao.py` direto. **Controle positivo/negativo (regra #227):** com gols dirigidos por força de time → `Over 2.5` incl **0.689** IC [0.54, 0.86]; com gols sorteados → incl **−0.12** IC [−0.40, 0.16] SEM RESOLUÇÃO. O instrumento discrimina — sem esse par, "nada tem resolução" seria indistinguível de backfill quebrado. **Vazamento temporal:** teste com a mesma história e última partida `0-0` vs `5-4` exige probabilidades **idênticas**; só o `outcome` muda. **Não mede** "a probabilidade que publicamos naquele dia" — não existe versão histórica dos `stats` da FootyStats; é a mesma família de modelo por outro caminho, e está dito no topo do módulo |
| 226-c | Corrigido | QA (Escanteios) | **A coluna não-zero: uma hipótese minha caiu, uma troca ficou provada.** 605 finalizadas da championship: `totalCornerCount` **100% não-zero**, `team_a/b_corners` 99%/98%, 12 colunas `odds_corners_*` em 99%, `corners_potential` **92% não-zero**. **Hipótese refutada:** eu havia dito que `corners_potential` seria enchimento porque o exemplo era `0` — era o primeiro valor da lista, não o típico; li exemplo como distribuição, o mesmo erro de amostra pequena que o #225-a existia para evitar. Os 8% de zeros também não são bug: `predictor.py:313` faz `if corners_potential > 0` antes do blend do #123, e produção lê `cornersPotential` dos **stats de time**, não da linha de partida (confundir as camadas foi o erro do #226-b). Nada a corrigir. **Troca comprovada:** `_extract_total_corners` procurava `totalCorners`/`total_corners`, que **não existem** — acertava pela soma dos times, pelo terceiro candidato. `totalCornerCount` promovido a primeiro nome **depois** de medir 605/605 de concordância entre total direto e soma: neutra comprovadamente, não plausivelmente. `team_a_corners_0_10_min` em 38% não-zero é o esperado (escanteio nos 10 primeiros minutos é raro) — a coluna existe justamente para não confundir isso com dado faltando |
| 227-a | Medido | QA (Instrumento) | **7860 picks reais, zero resolução em 15 células — e a referência que faltava.** championship: 605 finalizadas → 524 usadas → 7860 picks (gols 3144, escanteios 2620, cartões 2096), 4184 com EV. Todas as 15 células com IC cruzando zero (incl entre −0.25 e +0.22, n=524 cada). **A inclinação geral 0.78 é artefato de agregação** — mistura mercados com taxas-base diferentes (Over 0.5 quase sempre, Over 4.5 quase nunca) e essa separação vira "inclinação" no pooled; dentro da célula não sobra nada. **Não autoriza concluir ainda:** (1) a reconstrução é mais pobre que produção (declarado desde o #227); (2) com n=524 o IC tem meia-largura ~0.35 — **exclui resolução forte (≥0.4), não exclui modesta (0.2–0.3)**, então o veredito impresso "SEM RESOLUÇÃO" lê-se "sem resolução **demonstrável**"; (3) o instrumento pode errar em dado real. **Referência que separa as três:** cada pick ganha `prob_mercado` — de-vig real (#219) quando há par over/under (escanteios têm a escada completa, 99% no #226-c), `1/odd` marcado `implicita` quando só há a perna over. `--prob-de mercado` troca a fonte. **Caminho de mercado validado:** gerador passou a precificar do λ verdadeiro + 6% de margem → Over 2.5 incl **0.973** IC [0.80, 1.17], BTTS **0.990** IC [0.72, 1.28]. Isso calibra o **poder**: n desta ordem detecta inclinação 1 com meia-largura ~0.2, logo o zero real não é falta de amostra. **Previsão registrada antes de rodar:** escanteios entre 0.7 e 1.1; gols/BTTS mais baixo (margem embutida achata). Escanteios perto de zero ⇒ a suspeita vira o instrumento, não o modelo |
| 227-b | Corrigido | Fix (Veredito / Instrumento) | **O "zero" do mercado não era zero: era imprecisão, e o veredito somava as duas.** championship, prob do mercado: 4184 picks, **todos com de-vig real** (`implicita: 0` — o par over/under existe também em gols e BTTS, contra a minha previsão). GERAL incl **1.0096** IC [0.90, 1.12] "forma adequada". Mas 6 células saíram rotuladas **SEM RESOLUÇÃO com a inclinação pontual em 0.98, 0.98, 0.89, 0.82** — absurdo: a casa não é cega. Causa: `difere_de_0` só pergunta se o IC exclui zero, e **inclinação se estima pela variância de `logit(p)`** — o mercado varia pouco justamente por ser bem calibrado, então o IC é largo e cobre 0 **e** 1. `veredito` passa a separar: IC cobre 0 **e** 1 → **INCONCLUSIVO** (falta precisão; mais jogos estreitam, calibrar não); IC cobre 0 e **exclui** 1 → SEM RESOLUÇÃO. **Com o critério corrigido as 15 células do modelo continuam SEM RESOLUÇÃO** (todas excluem 1, teto 0.65) — o achado do #227-a sobrevive, o do mercado era rótulo. `--so-com-odd` recorta o modelo ao mesmo subconjunto do mercado (medir 7860 contra 4184 compara conjuntos, não previsores). **Instrumento que fecha a conta:** `scripts/comparar_com_mercado.py` — Brier e log-loss não dependem de espalhamento, e como os dois previsores olham as mesmas partidas a diferença é **emparelhada** (bloco = jogo), então o ruído comum se cancela. Traz também um **piso** (prever a taxa-base da célula): quem não bate o piso não usa informação nenhuma do jogo. **Validado apontando lados opostos:** no sintético, gols precificados do λ verdadeiro → MERCADO melhor (dif +0.014 a +0.017, IC exclui 0); escanteios com preço sorteado → MODELO melhor (dif −0.054 a −0.067). Comparador que apontasse o mesmo lado nos dois não mediria nada |
| 227-c | Medido | Resultado (Modelo) | **O modelo reconstruído perde para a taxa-base em todas as famílias com mercado.** championship, 4184 picks, os mesmos para os dois previsores, bootstrap emparelhado por jogo: Brier **modelo 0.2387 vs mercado 0.2219**, dif +0.0168 IC [+0.0118, +0.0218]; log-loss +0.0397 IC [+0.028, +0.052]. **Mercado melhor em 9 de 9 células**, IC excluindo zero em todas — escanteios entre as piores (+0.020 em Over 9.5/10.5). **Piso** (taxa-base desta amostra, o melhor constante possível): 0.2225 — **modelo −7.26%** (pior que não saber nada), mercado +0.30%. Inclinação do modelo nos mesmos 4184: 8/8 células SEM RESOLUÇÃO com IC excluindo 1. As duas medições contam a mesma história por caminhos independentes: a variação por jogo do modelo **adiciona** erro. **Prova:** o modelo desta família, alimentado por médias móveis, não carrega informação por jogo em gols/BTTS/escanteios da championship em 2 temporadas — passou pelos controles (#227), pela referência (#227-b) e pelo veredito corrigido. **Não prova** que produção esteja no mesmo lugar (xG, calibração per-liga, DC, γ, deflação não estão na reconstrução) — e a única forma de medir é o ledger (#218): `PREDICTION_LEDGER_ENABLED=1` vira caminho crítico. Hipótese registrada: a deflação (#043/#105) é o sistema tendo descoberto sozinho que suas probs cruas variam mais que a realidade. **Não fazer:** recalibrar (inclinação zero → calibrador devolve o piso, #220); concluir que o produto perde dinheiro (classificação, deflação e caps não estão na medição); estender a outras ligas sem rodar `--liga` |
| 227-e | Medido | Resultado (Modelo, 22 ligas) | **22 ligas, 64.718 picks: a championship era a regra.** Coleta 22/22, 2 temporadas, 127.775 picks reconstruídos, 64.718 com odd de-vigada. Brier **modelo 0.2383 vs mercado 0.2238**, dif +0.0145 IC [+0.0130, +0.0160]; log-loss +0.0362 IC [+0.032, +0.040]; piso 0.2249 → **modelo −5.97%**, mercado +0.47%. Mercado melhor em 9/9 mercados e em **21/22 ligas** (IC exclui 0); **modelo abaixo do piso em 21/22** — de −1.41% (la-liga) a −10.23% (ligue-1). A 22ª (premiership, +0.15%, n=1.736) é empate com IC cobrindo zero: "não dá para distinguir", não sinal. **Leitura mecânica:** excesso de Brier ≈ 0.0134 com inclinação ≈ 0 é ≈ variância das probs em torno da taxa-base → o modelo publica **~±12 pp por pick que não acompanham o desfecho**. O mercado fica a ±1% do piso em toda liga: o prêmio em disputa nessas famílias é ~1% de Brier, e o modelo está 6% do lado errado. **Previsões conferidas:** "championship é regra" ✓; "Bundesliga menos abaixo por ter mais gols" ✗ — é a **2ª pior** (−7.95%), o mecanismo suposto não existe; "liga acima do piso = olhar primeiro" ✗ — premiership é falta de amostra, não sinal. **Provado:** o modelo alimentado por médias móveis não carrega informação por jogo em gols/BTTS/escanteios, em 22 ligas. **Único buraco:** produção usa outros insumos — fechar pelo ledger (#218, mede produção de verdade) e/ou por um Poisson ingênuo nos mesmos jogos (isola motor de insumo, offline, uma tarde) |
| 228 | Corrigido | Fix (Ledger / Infra) | **O ledger podia gravar picks, mas nunca poderia pontuá-los.** Ao ligar `PREDICTION_LEDGER_ENABLED=1` (único caminho para medir produção após o #227-e), três lacunas: `garantir_tabelas()` **nunca chamado** (banco novo → INSERT falha → falha aberta engole em DEBUG → "ligado gravando nada"); `registrar_desfecho()` **nunca chamado** (`ledger_outcomes` vazia → JOIN devolve zero); `UNIQUE (match_id, market)` com `market` = **tipo** — um desfecho por tipo, e Over 1.5/Over 2.5/Under 2.5 do mesmo jogo receberiam o mesmo resultado: o ledger nunca poderia ter pontuado um pick. Correções: `_garantir_uma_vez()` com migração idempotente (`ADD COLUMN IF NOT EXISTS selection`, índice único por seleção); `registrar_desfechos_do_jogo` lê do próprio ledger o que foi publicado e pontua por seleção; gancho no batch audit após `actual_result`; `desfecho_do_pick` traduz para o avaliador determinístico — `Cards "Over 3.5"` sem prefixo seria lido como **gols**, `1X2 "Home"` não casa em nada; JOIN do `medir_inclinacao` por seleção. `scripts/lambda_env.py`: lê-mescla-grava (o `update-function-configuration --environment` inline **apaga** as outras variáveis), diff mascarado, pré-check Active/Successful, `file://` (vírgula e `=` em `DATABASE_URL`). Validado com `aws` dublê. **Não verificável daqui:** psycopg2 no pacote e `DATABASE_URL` alcançável — se faltar, a flag diz ligado e grava nada. Prova de vida: `[#218] ledger: N de M` e `[#228] desfechos: N/M` no CloudWatch. 14 testes sem Postgres (psycopg2 stub no `sys.modules` — senão o resultado dependeria da máquina) |
| 229 | Implementado | Instrumento (Backfill) | **Poisson ingênuo como terceiro previsor: isolar o motor do insumo.** O #227-e provou −6% sobre o piso em 22 ligas, mas não diz se o limite é o **insumo** (médias móveis sem informação) ou o **motor** (médias com informação que o motor destrói) — decisões opostas. Cada pick ganha `prob_ingenuo`: Poisson direto sobre as **mesmas** médias (λ = casa em casa + fora fora, sem força relativa/xG/EMA/encolhimento/γ). A única diferença para `prob` é o motor; a diferença emparelhada de Brier tem três leituras impressas: IC>0 **motor destrói sinal**; cobre 0 **limite no insumo**; IC<0 **motor extrai algo**. **Validado no controle positivo** (4307 picks): motor +6.48%, ingênuo +1.60%, mercado sorteado −8.08%; motor−ingênuo −0.0111 IC [−0.016, −0.006] → motor melhor onde há sinal; escanteios empate. O ingênuo **acha** o sinal plantado (inclinação IC>0.15, é teste) — senão "empate" seria dois cegos empatando. **Previsão registrada:** nas 22 ligas, empate na maioria (insumo é o limite), motor ligeiramente pior em gols ("extremas demais" do #227-c). Motor melhor em dado real ⇒ primeira evidência de que ele extrai algo |
| 229-a | Corrigido | Fix (Instrumento) | **Rodada real do ingênuo: gols empatam, escanteios "motor melhor" — e o rótulo mentia.** 22 ligas, 64.706 picks: motor −5.97%, ingênuo **−9.60%**, mercado +0.47%; motor−ingênuo −0.0082 IC [−0.0092, −0.0071]. **Gols: empate** (Over 2.5 −0.0004, Over 3.5 +0.0004) — força relativa, EMA, encolhimento e γ não movem o Brier sobre este insumo; o motor de gols é inerte. **Escanteios: motor melhor nas 5 linhas** (−0.011 a −0.014, IC estreito). O script imprimiu "o motor extrai algo" — **não sustentado**: o #227-c/e mediu inclinação ≈ 0 em todas as células de escanteios do motor, e dois previsores sem resolução diferem em Brier **só pelo espalhamento**. O motor de escanteios encolhe (redução 20% #043, teto 12); o ingênuo soma médias cruas. Correção: decomposição exata por célula `Brier − piso = espalhamento − 2·sinal`; a diferença motor−ingênuo é atribuída a **sinal** ou **espalhamento**, e "extrai algo" só sai quando o sinal domina. Validado nos dois controles (com sinal: −0.0088 dos −0.0111 por sinal; sem sinal: ≈ 0). Identidade fecha a 1e-9 (teste). Ledger ligado (`+ PREDICTION_LEDGER_ENABLED = 1`, 19 preservadas) — prova de vida só no CloudWatch. **Previsão registrada:** sinal do motor ≈ 0 em escanteios, "ganha por ENCOLHER". Lição: instrumento validado só onde a hipótese vale não está validado, está confirmado |
| 229-b | Medido | Resultado (Modelo — fecha o #220) | **A decomposição no dado real: o motor ganha por encolher, e o teto de calibração é o piso.** 64.706 picks: espalhamento/sinal — motor 0.0166/+0.0016, ingênuo 0.0260/+0.0022, mercado **0.0032/+0.0021**. Diferença motor−ingênuo (−0.0082): +0.0012 por sinal (o motor tem **menos** sinal que o Poisson direto), −0.0094 por espalhamento → **"ganha por ENCOLHER"**. Previsão do #229-a confirmada nas três partes. `sinal/espalhamento` ≈ inclinação linear: motor **0.10**, mercado **0.66** — o mesmo "≈ zero" do #227 por caminho independente. O mercado carrega covariância da mesma ordem com **5× menos variância**: o modelo publica ±12.9 pp por pick e 10% acompanham a realidade; o mercado ±5.7 pp. **Teto após calibração linear ótima** (`piso − sinal²/espalhamento`): motor **+0.07%**, ingênuo +0.08%, mercado +0.61%. Calibrar — isotônica, Platt, Beta, deflação — devolve o piso com outro nome; o #220 previu em teoria, agora é número. Explica por que a deflação (#043/#105) melhorava auditorias: caminha para o encolhimento ótimo, cujo destino é o piso. **Fechado para a reconstrução:** não há informação a extrair (64k picks, dois controles, referência, decomposição exata). **Aberto só isto:** produção come `stats` da FootyStats, não médias móveis — o ledger (ligado hoje) mede. Decisão de produto registrada como tal: ancorar no mercado de-vigado (#219) ou publicar a taxa-base rotulada; nenhuma é "calibrar melhor" |
| 230 | Implementado (sombra) | Decisão de produto + Instrumento | **Ancorar a probabilidade no mercado de-vigado — passo 1: o ledger grava as duas.** Decisão do Welligton sobre #227-e/#229-b (modelo −5.97% vs mercado +0.47%; teto de calibração +0.07% vs +0.61%); estende às outras famílias o que o #187 já fez no 1X2. **Consequência registrada:** EV contra o mesmo mercado vira zero por construção; o valor que sobra é entre casas (#120); SAFE/NEUTRO precisa ser redefinida em valor + confiança na âncora — passo 4, **fora deste patch**. Este patch: o ledger grava `prob_mercado` (Shin sobre o par over/under, `1/odd` se só a própria perna), `mercado_metodo`, `odd_par`, `margem_pp`, `frescor` (#219) ao lado da publicada, no mesmo instante, com nomes de odds **medidos** em `routes/fixtures.py` (teste por família; `Cards "Over 3.5"` não vira âncora de gols). Odd do par entra no hash (preço que mexeu = revisão). `comparar_com_mercado.py --ledger` mede produção contra mercado nos mesmos picks. **Modo sombra puro** — nada publicado muda. **Gate (regra #230):** trocar a fonte só com Brier(mercado) < Brier(publicada) IC excluindo 0, teto da publicada < 0.25%, n ≥ 300 jogos; peso do modelo = mistura linear ótima medida. Previsão: peso zero |
| 230-d | Corrigido | Fix (Instrumento) + Prova de vida | **Ledger vivo: 728 picks reais — e dois defeitos do instrumento na primeira leitura.** `--ledger` devolveu 728 picks com desfecho: ledger grava, batch audit pontua por seleção, âncora de mercado gravada. Primeiro número de produção (~90 jogos): Brier **modelo 0.1958 vs mercado 0.2017**, dif −0.0059 IC [−0.022, +0.008] — **inconclusivo**, como a regra #230 previa ao exigir 300 jogos; o IC cobre a diferença da reconstrução e cobre zero. Ponto favorece a publicada, plausível sem sinal: `calibrated_prob` é a **deflacionada** (#106) e a deflação caminha para o encolhimento ótimo (#229-b). **Defeito 1:** piso 0.0525 e "modelo −272%" — ótimo in-sample com células de 1–5 picks (taxa 0 ou 1). Correção: célula com n<30 cai para mercado agregado, depois total; mesma regra na decomposição. **Defeito 2:** vereditos com n=4 e `TypeError` na tabela por liga (skill None no sort). Abaixo de 30, "sem veredito". **Cobertura estranha:** Over 4.5 88, Over 1.5 87, Draw 79, Home 21, **Over 2.5 = 5**. Ou o pipeline publica assim (`_filter_1x2_dc_redundancy`, corredor #187) ou as linhas somem num filtro — tabela COBERTURA DO LEDGER por seleção, filtro a filtro, responde |
| 230-e | Corrigido | Fix (Âncora / Cobertura) | **A cobertura respondeu: Over 2.5 quase não é publicado, e a âncora tinha dois buracos.** Ledger real, 728 picks: Over 2.5 tem **31 linhas** contra 300 de Over 1.5 (corredor #187); Home 59 contra 222 de Draw (`_filter_1x2_dc_redundancy` troca por DC). A cobertura do ledger é a do **produto** — achado registrado. **Buracos meus:** Dupla Chance com **0** `prob_mercado` em 831 linhas (`par_de_odds` não tratava); 1X2 100% `implicita` — Shin generaliza para 3 pernas e eu não usei. Correção: 1X2 e DC por de-vig de **três pernas** (`devig3`), DC = soma de duas pernas; somam 1.000 contra 1.056 da implícita. **Viés a favor do modelo:** a comparação misturava de-vigado com 1/odd (margem 5–7 pp dentro), inflando o Brier do mercado — Draw n=79 entrava assim. `--ledger` passa a usar só `devig`/`devig3`; `--incluir-implicita` abre. O "modelo −0.006 melhor" do #230-d fica **retirado**. **n de picks ≠ n de jogos:** um jogo grava ~20 seleções; 728 picks eram ~40 jogos. `jogos` impresso em toda tabela; por liga exige 10 jogos. Previsão: dif entre −0.005 e +0.010, IC cobrindo zero, ponto deslocado para o mercado |
| 230-f | Corrigido | Fix (Instrumento) + Medição | **Primeira leitura limpa de produção: previsão confirmada, uma circularidade e um "significativo" isolado.** Ledger real, só de-vigados: desde 03/09 **1906 picks em 131 jogos**, dif Brier **+0.0041 IC [−0.0020, +0.0101]**; desde 05/09 +0.0064 IC [+0.0007, +0.0128], 106 jogos. Previsão do #230-e conferida (ponto de −0.006 para +0.004..+0.006). Inconclusivo por regra (#230 exige 300 jogos); janelas aninhadas = uma medição. Piso ~0.209: modelo −2.7% a −3.9%, mercado −0.7% a −1.2%. **Circularidade:** `Draw dif +0.0002 IC [−0.0036, +0.0048]` é identidade — em produção 1X2 vem da odd (`odds_implied`, fixtures_service:1884) e DC é soma normalizada das pernas (market_service:300). 537/1906 picks; fora do TODAS por padrão, marcados `[CIRCULAR]`. **"Over 2.5 MODELO melhor"** (29 jogos, 1 de 13 células): p-valor bootstrap + **Benjamini-Hochberg** por célula; "passa BH" só se sobreviver. Viés de seleção por cima: o corredor #187 só publica Over 2.5 quando o pipeline confia. **Cobertura:** Cards O2.5/O1.5 e Corners O/U 4.5 com **zero preço** (mercados publicados em volume sem referência possível → taxa-base rotulada pela decisão #230); unders só via enriquecimento (24%); escanteios quase sem par (Over 11.5: 8/357). Over 2.5 publicado em 68 de ~450 |
| 230-g | Corrigido | Fix (Mapper) | **O par de escanteios estava no payload; o mapper jogava fora.** No #230-f escrevi que a âncora de escanteios precisaria de "outra fonte" — errado por não conferir o código: a FootyStats manda over **e** under de 7.5 a 11.5 (99% de 605, #226-c) e o record lia só quatro overs. Os unders morriam no mapper (`fixtures_service`), mesma forma do #226-b. Record passa a publicar `cornersOver75` e `cornersUnder75..115`, nomes iguais aos do enriquecimento #120; `par_de_odds` já os esperava. Teste monta record real a partir da escada inteira: `Corners Over 9.5` sai `devig`, over+under de-vigados somam 1. Manifesto #210 fecha. Efeito esperado: escanteios de ~8 para centenas de pares no ledger. `diagnostico_chaves_escanteios.py --contem odds_` lista toda odd da linha para saber quais unders de gols existem |
| 230-h | Corrigido | Fix (Mapper) + Correção de medição | **A FootyStats manda tudo: unders de gols, Dupla Chance e o 1X2 que o #225-a mediu em "0%".** `--contem odds_` em 617 finalizadas: `odds_ft_1/x/2` 99%, over e under de 0.5 a 4.5 99%, `odds_doublechance_*` 99%, escada de escanteios 7.5–11.5 99%; **não existem** `odds_cards_*` nem escanteios 4.5/5.5/6.5/12.5. Três nomes errados no record: 1X2 lia `odds_ft_home_team_win` (inexistente) — foi por isso que o #225-a mediu **1X2 em 0%** e o #227 tirou o 1X2 do backfill "por falta de odd"; a odd existia. Unders de gols: só `under25` publicado (Under 1.5 com âncora em 24%). DC: nenhuma odd publicada. Corrigido com os nomes do #120; backfill cobre as cinco linhas de gols. 1X2 segue fora da comparação modelo × mercado pelo motivo certo (#230-f, circular). "Mercado sem preço" confirmado por inventário: cartões e escanteios 4.5 não têm referência em nenhuma fonte. Fixture do #225-a fabricava a chave que o código esperava (regra #222 ponto 6) |
| 231 | Implementado (flag desligada) | Feature (Fonte da probabilidade) | **`PROB_SOURCE=mercado`: a troca da fonte existe atrás de flag, desligada.** Item 1 do passo 4 do #230. Ligada, `calibrated_probability` de cada seleção com par de-vigado (`devig`/`devig3`) vira a `prob_mercado` do #230; sem par cai para a taxa-base da célula (liga, mercado, seleção; n ≥ 30, fallback `*`) rotulada `taxa_base`; sem taxa-base o modelo fica rotulado `modelo_sem_referencia`. `implicita` nunca ancora (margem inteira). Modelo preservado em `model_probability`; EV `None` até o item 2; classificação intocada até o item 3. **Ledger:** `calibrated_prob` segue sendo o modelo; novas colunas `published_prob`/`prob_source`; `--campo published_prob` no comparador. Taxa-base: `backfill --todas` → `gerar_taxas_base.py` → `backend/config/taxas_base.json` (gerar com a chave). Backfill passou a cobrir escanteios 4.5–6.5/12.5 e cartões 1.5. Prova: 20 seleções antes/depois, 15 `mercado` + 5 sem referência; flag off = payload idêntico. **Proibido ligar antes do gate #230 (300 jogos) e dos itens 2–3** |
